import '../Store/actions.dart';
import '../Models/AppData.dart';

AppData appDataStore(AppData previousState, dynamic action) {
  //if (action == Actions.increment) {
  // return previousState + 1;
  //}

  return previousState;
}
